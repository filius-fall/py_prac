# from expiringdict import ExpiringDict

# user_cache = ExpiringDict(max_len = 50,max_age_seconds = 30,items = None)

# user_cache['user_one'] = 'Alfred'
# user_one = user_cache.get('user_one',default="Nothing Found",with_age = True)
# print(user_one)

# user_cache['user_two'] = "foo"
# user_two = user_cache.get('user_two',with_age = True)

# user_with_timestamps = user_cache.items_with_timestamp()
# print(user_with_timestamps)

assert 0
print('2')